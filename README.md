# Мультиплеерная викторина (QuizBattle)

**Срок реализации** - 3 месяца

**Команда** - 2 специалиста: Архитектор/Программист + Тестировщик/Scrum-мастер

---

## Содержание

1. [Описание проекта](#1-описание-проекта)
2. [Роли в команде](#2-роли-в-команде)
3. [Роли пользователей](#3-роли-пользователей)
4. [Функциональные требования](#4-функциональные-требования)
5. [Технический стек](#5-технический-стек)
6. [Архитектура и модель данных](#6-архитектура-и-модель-данных)
7. [API Endpoints](#7-api-endpoints)
8. [WebSocket Events](#8-websocket-events)
9. [Распределение задач](#9-распределение-задач)
10. [Процесс разработки (Scrum)](#10-процесс-разработки-scrum)
11. [Критерии приемки](#11-критерии-приемки)
12. [Таймлайн](#12-таймлайн)

---

## 1. Описание проекта

Веб-приложение для проведения интеллектуальных викторин в реальном времени. Пользователи создают комнаты, приглашают друзей и соревнуются в знаниях.

### Основные возможности

- Создание и участие в игровых комнатах
- Real-time игровой процесс с синхронизацией
- Система подсчета очков (правильность + скорость)
- Личная статистика и глобальный рейтинг
- База вопросов с категориями

---

## 2. Роли в команде

### Разработчик 1: Архитектор и Программист (Backend + Frontend Lead)

| Область | Ответственность |
|---------|-----------------|
| **Архитектура** | Проектирование системы, выбор технологий, схема БД, API дизайн |
| **Backend** | Разработка сервера, REST API, WebSocket, игровая логика, база данных |
| **Frontend** | Создание React приложения, state management, интеграция API, UI компоненты |
| **DevOps** | Docker, деплой, CI/CD настройка |
| **Документация** | Техническая документация, Swagger, README |

### Разработчик 2: Тестировщик и Scrum-мастер

| Область | Ответственность |
|---------|-----------------|
| **Тестирование** | Написание тестов (unit, integration, e2e), ручное тестирование, баг-трекинг |
| **QA процессы** | Создание тест-планов, чеклистов, сценариев тестирования |
| **Scrum-мастер** | Ведение спринтов, daily meetings, ретроспективы, управление задачами в Jira/Trello |
| **Документация** | Пользовательская документация, инструкции, отчеты о тестировании |
| **UX/UI** | Проверка интерфейса, юзабилити-тестирование, обратная связь |

---

## 3. Роли пользователей

| Роль | Возможности |
|------|-------------|
| **Гость** | Просмотр главной страницы, правил |
| **Игрок** | Регистрация, вход, создание комнат, участие в играх, статистика, лидерборд |
| **Хост** | Настройка комнаты, запуск игры, управление игроками |
| **Админ** | Управление вопросами, просмотр статистики, модерация |

---

## 4. Функциональные требования

### 4.1. Авторизация и профиль

**Регистрация/вход:**
- По email/паролю
- JWT токены
- Валидация данных

**Профиль:**
- Аватар (генерация по умолчанию)
- Никнейм
- Дата регистрации

**Статистика игрока:**
- Всего игр
- Побед
- Процент правильных ответов
- Общий рейтинг

### 4.2. Комнаты (лобби)

**Создание комнаты:**
- Название
- Тип доступа: публичная / приватная (по коду)
- Максимум игроков (2-8)
- Количество вопросов (5, 10, 15)
- Категория (Наука, История, Спорт, Кино, Микс)
- Время на ответ (10, 20, 30 сек)

**Список комнат:**
- Публичные комнаты
- Поиск по названию
- Фильтр по статусу

**Лобби комнаты:**
- Список игроков с аватарками
- Индикаторы готовности
- Чат комнаты
- Кнопка старта (только для Host)

### 4.3. Игровой процесс

**Этапы:**

1. **Обратный отсчет** (3 сек)
2. **Раунд** (повторяется N раз):
   - Отображение вопроса и 4 вариантов ответа
   - Таймер с визуальным прогресс-баром
   - Игрок выбирает ответ → кнопки блокируются
   - Сервер начисляет очки
   - Показ правильного ответа и очков за раунд (3 сек)
3. **Завершение:**
   - Финальная таблица с результатами
   - Кнопки: "Играть снова", "В лобби", "На главную"

**Формула очков:**
```
Правильный ответ: 100 + (оставшееся время / макс. время) × 50
Неправильный ответ: 0
```

### 4.4. Лидерборд

- Топ-100 игроков по сумме очков
- Отображение места текущего пользователя

### 4.5. База вопросов

**Структура:**
- Текст вопроса
- 4 варианта ответа
- Правильный ответ (0-3)
- Категория
- Сложность (easy, medium, hard)

**Наполнение:** минимум 100 вопросов при старте

**Админка:** CRUD для вопросов

---

## 5. Технический стек

| Компонент | Технология |
|-----------|------------|
| **Frontend** | React + TypeScript + Vite |
| **UI** | Material UI / Chakra UI |
| **State** | Zustand |
| **Backend** | Node.js + Express.js + TypeScript |
| **Real-time** | Socket.io |
| **Database** | PostgreSQL |
| **Cache** | Redis |
| **ORM** | Prisma |
| **Testing** | Jest, React Testing Library, Playwright |
| **Docker** | Docker + Docker Compose |
| **CI/CD** | GitHub Actions |

---

## 6. Архитектура и модель данных

### 6.1. Схема базы данных

```sql
-- Пользователи
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  nickname VARCHAR(50) UNIQUE NOT NULL,
  email VARCHAR(255) UNIQUE NOT NULL,
  password_hash VARCHAR(255) NOT NULL,
  avatar_url TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  total_score INTEGER DEFAULT 0,
  games_played INTEGER DEFAULT 0,
  wins INTEGER DEFAULT 0,
  correct_answers INTEGER DEFAULT 0
);

-- Категории
CREATE TABLE categories (
  id SERIAL PRIMARY KEY,
  name VARCHAR(50) UNIQUE NOT NULL
);

-- Вопросы
CREATE TABLE questions (
  id SERIAL PRIMARY KEY,
  text TEXT NOT NULL,
  options JSONB NOT NULL,
  correct_answer INTEGER NOT NULL CHECK (correct_answer BETWEEN 0 AND 3),
  category_id INTEGER REFERENCES categories(id),
  difficulty VARCHAR(10) CHECK (difficulty IN ('easy', 'medium', 'hard'))
);

-- Комнаты
CREATE TABLE rooms (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  code VARCHAR(6) UNIQUE NOT NULL,
  name VARCHAR(100) NOT NULL,
  host_id UUID REFERENCES users(id),
  status VARCHAR(20) DEFAULT 'waiting',
  settings JSONB NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Игроки в комнате
CREATE TABLE room_players (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  room_id UUID REFERENCES rooms(id) ON DELETE CASCADE,
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  is_ready BOOLEAN DEFAULT false,
  score INTEGER DEFAULT 0,
  UNIQUE(room_id, user_id)
);

-- История игр
CREATE TABLE games (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  room_id UUID REFERENCES rooms(id),
  winner_id UUID REFERENCES users(id),
  started_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  finished_at TIMESTAMP
);

-- Ответы
CREATE TABLE game_answers (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  game_id UUID REFERENCES games(id) ON DELETE CASCADE,
  user_id UUID REFERENCES users(id),
  question_id INTEGER REFERENCES questions(id),
  answer INTEGER CHECK (answer BETWEEN 0 AND 3),
  is_correct BOOLEAN,
  time_spent INTEGER,
  points_earned INTEGER
);

-- Лидерборд
CREATE MATERIALIZED VIEW leaderboard AS
SELECT 
  u.id,
  u.nickname,
  u.avatar_url,
  ROW_NUMBER() OVER (ORDER BY u.total_score DESC) as rank,
  u.total_score,
  u.wins,
  CASE 
    WHEN u.games_played > 0 
    THEN (u.correct_answers::float / (u.games_played * 10) * 100)
    ELSE 0 
  END as accuracy
FROM users u
WHERE u.games_played > 0
ORDER BY u.total_score DESC
LIMIT 100;
```

### 6.2. Redis структуры

```
# Состояние комнаты
room:{roomId} -> hash {
  status: "waiting|playing",
  currentQuestion: index,
  questionStartTime: timestamp
}

# Очки игроков
room:{roomId}:scores -> hash {
  userId: score
}

# Ответы за раунд
room:{roomId}:answers:{questionId} -> list [
  {userId, answer, timeSpent}
]
```

---

## 7. API Endpoints

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| POST | `/api/auth/register` | Регистрация |
| POST | `/api/auth/login` | Вход |
| GET | `/api/users/:id` | Профиль |
| GET | `/api/users/:id/stats` | Статистика |
| POST | `/api/rooms` | Создать комнату |
| GET | `/api/rooms` | Список комнат |
| GET | `/api/rooms/:id` | Информация о комнате |
| POST | `/api/rooms/:code/join` | Присоединиться |
| GET | `/api/leaderboard` | Лидерборд |
| GET | `/api/categories` | Категории |
| POST | `/api/admin/questions` | Добавить вопрос (admin) |

---

## 8. WebSocket Events

**Пространство имен:** `/game`

### Клиент → Сервер

| Событие | Данные |
|---------|--------|
| `join_room` | `{ roomId, userId }` |
| `player_ready` | `{ roomId, isReady }` |
| `start_game` | `{ roomId }` |
| `submit_answer` | `{ roomId, questionId, answerIndex, timeSpent }` |
| `chat_message` | `{ roomId, message }` |

### Сервер → Клиент

| Событие | Данные |
|---------|--------|
| `room_state` | `{ players, settings }` |
| `player_joined` | `{ user, players }` |
| `player_left` | `{ userId, players }` |
| `game_starting` | `{ countdown }` |
| `new_question` | `{ text, options, timeLimit, current, total }` |
| `answer_result` | `{ isCorrect, pointsEarned, correctAnswer }` |
| `round_summary` | `{ correctAnswer, results }` |
| `game_over` | `{ results, winner }` |

---

## 9. Распределение задач

### Разработчик 1: Архитектор и Программист

| Неделя | Задачи |
|--------|--------|
| 1 | Проектирование архитектуры, настройка Docker, PostgreSQL, Prisma |
| 2 | Разработка Auth (JWT), базовая структура проекта |
| 3 | REST API: пользователи, профили, статистика |
| 4 | REST API: комнаты, категории, вопросы |
| 5 | WebSocket сервер, подключение комнат |
| 6 | Игровая логика, таймеры, синхронизация |
| 7 | Система подсчета очков, Redis |
| 8 | Лидерборд, оптимизация запросов |
| 9 | Админка (API), документация Swagger |
| 10 | Frontend: настройка React, роутинг, UI библиотека |
| 11 | Frontend: авторизация, профиль, создание комнат |
| 12 | Frontend: лобби, чат, игровой экран, лидерборд |

### Разработчик 2: Тестировщик и Scrum-мастер

| Неделя | Задачи |
|--------|--------|
| 1 | Настройка Jira/Trello, создание бэклога, спринт-планирование |
| 2 | Разработка тест-плана, написание чеклистов |
| 3 | Ручное тестирование API (Postman коллекции) |
| 4 | Настройка Jest, написание unit тестов |
| 5 | Тестирование WebSocket, проверка синхронизации |
| 6 | Интеграционное тестирование (API + DB) |
| 7 | Тестирование игровой логики, проверка формулы очков |
| 8 | Нагрузочное тестирование (k6 / artillery) |
| 9 | E2E тестирование (Playwright) |
| 10 | Проведение ретроспектив, ведение баг-трекинга |
| 11 | Пользовательское тестирование (UAT), сбор обратной связи |
| 12 | Регрессионное тестирование, подготовка отчета |

---

## 10. Процесс разработки (Scrum)

### Структура спринтов (4 спринта по 3 недели)

| Спринт | Цель | Задачи Архитектора | Задачи Тестировщика |
|--------|------|-------------------|-------------------|
| **Sprint 1** | Базовый backend | Auth, пользователи, БД | Тест-план, Postman коллекции |
| **Sprint 2** | API и WebSocket | Комнаты, вопросы, WS | Unit тесты, тестирование API |
| **Sprint 3** | Игровая логика | Игровой процесс, Redis, очки | Интеграционные тесты, WS тесты |
| **Sprint 4** | Frontend и финал | React приложение, интеграция | E2E тесты, UAT, багфиксинг |

### Ежедневные активности

| Время | Активность | Участники |
|-------|------------|-----------|
| 10:00 | Daily Standup (15 мин) | Оба |
| 10:15-13:00 | Разработка / Тестирование | Оба |
| 13:00-14:00 | Обед | - |
| 14:00-18:00 | Разработка / Тестирование | Оба |

### Еженедельные активности

| День | Активность |
|------|------------|
| Понедельник | Планирование спринта (обновление задач) |
| Пятница | Демо, ретроспектива, обновление бэклога |

---

## 11. Критерии приемки

### Функциональные критерии

| Критерий | Проверка |
|----------|----------|
| Регистрация и вход работают | Тестировщик |
| Создание комнаты с настройками | Тестировщик |
| Присоединение по коду или из списка | Тестировщик |
| Лобби показывает игроков и статус готовности | Тестировщик |
| Игра запускается (только Host) | Тестировщик |
| Вопросы и варианты отображаются корректно | Тестировщик |
| Таймер синхронизирован у всех игроков | Тестировщик |
| Очки начисляются по формуле | Тестировщик |
| Финальная таблица результатов | Тестировщик |
| Статистика сохраняется в профиль | Тестировщик |
| Лидерборд показывает топ игроков | Тестировщик |
| Админ может управлять вопросами | Тестировщик |

### Критерии качества

| Критерий | Ответственный |
|----------|---------------|
| Покрытие кода тестами > 70% | Тестировщик |
| Нет критических багов | Тестировщик |
| TypeScript strict mode | Архитектор |
| Время ответа API < 200ms | Тестировщик |
| WebSocket reconnect работает | Тестировщик |
| Docker сборка работает | Архитектор |
| Swagger документация актуальна | Архитектор |

### Критерии завершения спринта

- [ ] Все запланированные задачи выполнены
- [ ] Нет критических багов в новой функциональности
- [ ] Тесты проходят успешно
- [ ] Демо показано заказчику
- [ ] Ретроспектива проведена

---

## 12. Таймлайн (12 недель)

### Неделя 1-3: Фундамент

| Неделя | Архитектор | Тестировщик |
|--------|------------|-------------|
| 1 | Архитектура, Docker, PostgreSQL, Prisma | Настройка Jira, создание бэклога |
| 2 | Auth (JWT), регистрация, логин | Тест-план, Postman коллекции |
| 3 | Пользователи, профили, статистика | Ручное тестирование API |

### Неделя 4-6: API и WebSocket

| Неделя | Архитектор | Тестировщик |
|--------|------------|-------------|
| 4 | CRUD комнат, категорий, вопросов | Настройка Jest, unit тесты |
| 5 | WebSocket сервер, подключение комнат | Тестирование WebSocket |
| 6 | Игровая логика, таймеры | Интеграционные тесты |

### Неделя 7-9: Игровой процесс

| Неделя | Архитектор | Тестировщик |
|--------|------------|-------------|
| 7 | Подсчет очков, Redis, лидерборд | Тестирование игровой логики |
| 8 | Админка API, оптимизация | Нагрузочное тестирование |
| 9 | Swagger, документация | E2E тестирование (Playwright) |

### Неделя 10-12: Frontend и финал

| Неделя | Архитектор | Тестировщик |
|--------|------------|-------------|
| 10 | React, роутинг, UI библиотека | Проведение ретроспектив |
| 11 | Авторизация, профиль, комнаты | Пользовательское тестирование (UAT) |
| 12 | Лобби, чат, игровой экран | Регрессия, отчет, деплой |

---

## Приложение A: Пример вопроса

```json
{
  "id": 1,
  "text": "Сколько планет в Солнечной системе?",
  "options": ["7", "8", "9", "10"],
  "correctAnswer": 1,
  "category": "Наука",
  "difficulty": "easy"
}
```

## Приложение B: Подсчет очков

```typescript
function calculatePoints(
  isCorrect: boolean,
  timeLeft: number,
  maxTime: number
): number {
  if (!isCorrect) return 0;
  const speedBonus = (timeLeft / maxTime) * 50;
  return Math.round(100 + speedBonus);
}
```

## Приложение C: Шаблон тест-кейса

| ID | Название | Предусловия | Шаги | Ожидаемый результат |
|----|----------|-------------|------|---------------------|
| TC-001 | Регистрация | - | 1. Открыть страницу регистрации<br>2. Ввести email, nickname, password<br>3. Нажать "Зарегистрироваться" | Пользователь создан, автоматический вход |

## Приложение D: Отчетность

### Еженедельный отчет тестировщика

- Количество найденных багов
- Количество закрытых багов
- Критические баги (список)
- Прогресс покрытия тестами
- Риски и блокеры

### Еженедельный отчет архитектора

- Завершенные задачи
- Технические решения
- Изменения в архитектуре
- Риски и блокеры

---

**Ключевые принципы:**
- Архитектор отвечает за качество кода и технические решения
- Тестировщик отвечает за качество продукта и процессы
- Scrum-процессы обеспечивают прозрачность и предсказуемость
- Каждый спринт завершается работающим инкрементом продукта
